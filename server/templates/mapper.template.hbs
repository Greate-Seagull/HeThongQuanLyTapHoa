import { {{titleCase name}} } from "../../domain/{{kebabCase name}}";

export class {{titleCase name}}Mapper {
	static toDomain(raw: any) {
		return {{titleCase name}}.rehydrate(raw);
	}

	static toPersistence(entity: {{titleCase name}}) {
		return {
			
		};
	}
}