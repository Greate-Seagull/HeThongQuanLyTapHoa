import { prisma } from "../../src/composition-root";
import { send } from "./{{kebabCase name}}.test-data";

jest.setTimeout(20000);

describe("{{normalCase name}} integration test", () => {
	let input;
	let output;

	beforeAll(async () => {

	});

	afterAll(async () => {
		
	});

	describe("Normal case", () => {
		beforeAll(async () => {
			input = send;
			output = await {{camelCase name}}Usecase.execute(input);
		});

		it("Should return 200", () => {
			expect(output.status).toBe(200);
		});
	});
});
