import request from "supertest";
import app from "../../src/app";
import { prisma } from "../../src/composition-root";
import { send } from "./{{kebabCase name}}.test-data";

jest.setTimeout(20000);

describe("{{normalCase name}} integration test", () => {
	let path = `{{apiURI}}`;
	let input;
	let output;

	beforeAll(async () => {

	});

	afterAll(async () => {
		
	});

	describe("Normal case", () => {
		beforeAll(async () => {
			input = send;
			output = await request(app).{{lowerCase apiMethod}}(path).send(input);
		});
	});
});
