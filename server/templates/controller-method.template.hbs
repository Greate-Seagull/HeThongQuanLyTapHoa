$&
export async function control{{titleCase name}}(req, res) {
	try {
		console.log("Call {{upperCase apiMethod}} {{apiURI}}");

		let input = req.body;
		const output = await {{camelCase name}}Usecase.execute(input);
		res.json(output);

		console.log("Return {{upperCase apiMethod}} {{apiURI}}");
	} catch (e: any) {
		console.error(e.message);
		res.status(400).json({ message: e.message });
	}
}
